<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcy.login.mapper.userMpper">

	<select id="getUserAccount" resultType="com.kcy.project.security.userVo">
		SELECT * FROM k_users
        WHERE USER_ID= #{userId}
	</select>
	
</mapper>